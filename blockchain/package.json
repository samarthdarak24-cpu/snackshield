{
  "name": "snackshield-blockchain",
  "version": "1.0.0",
  "description": "SnackShield Smart Contracts - AI & Blockchain Fake Product Detection",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "npx hardhat run scripts/deploy.js --network sepolia",
    "node": "npx hardhat node",
    "seed": "npx hardhat run scripts/seed.js --network localhost",
    "verify": "npx hardhat run scripts/verify.js --network localhost"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.22.0"
  }
}
